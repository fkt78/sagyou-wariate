# 指示書：客数・売上入力時の客単価チェック

**対象アプリ**：CONV-SAGYOU-APP（作業割当アプリ）
**バージョン**：v1.4.7 → **v1.5.0**
**作成日**：2026-08-04

---

## 1. 目的

客数・売上の一括入力時に、**売上欄と客数欄の取り違え**を入力したその場で検出する。

実データを調査したところ、過去10か月で**7件の取り違えが見つかった**（うち6件が伊賀平野東町店）。いずれも入力から今日まで誰も気づいていなかった。

例：2026-07-24 伊賀平野東町店
- 作業割当アプリ：売上 887,000円 / 客数 718人 → 客単価 **1,235円**
- 日販廃棄アプリ：売上 720,000円 / 客数 887人 → 客単価 812円
- 同日の他店は 774円・733円 で正常 → **実際のイベントではなく入力ミス**

客単価が通常の範囲を外れたら注意を出すことで、その場で気づけるようにする。

---

## 2. 重要な前提

### 2-1. 警告であって、ブロックではない

**客単価が跳ねる「本物の日」が実在する。** 実データで確認済み：

| 日付 | 東町店 | 北谷店 | 南駅店 |
|---|---|---|---|
| 2025-12-24（クリスマスイブ） | 1,148円 | 1,013円 | 798円 |
| 2025-12-31（大晦日） | 1,078円 | 851円 | 928円 |
| 2026-03-31 | 1,092円 | 1,066円 | 874円 |

ケーキやオードブルの予約引き渡しで客単価が上がる。**これらの日にも警告は出るが、それで正しい。**

担当者が「今日はクリスマスだから正常」と判断してそのまま保存できること。**保存を止めてはならない。**

### 2-2. 固定のしきい値では機能しない

時間帯によって「普通」が大きく違う。実測値：

| 時 | 中央値 | 95%点 | 最大 | 客数の中央値 |
|---|---|---|---|---|
| 2時 | 750円 | 2,000円 | **10,000円** | 3人 |
| 8時 | 636円 | 939円 | 1,529円 | 36人 |
| 20時 | 912円 | 1,261円 | 1,698円 | 39人 |

深夜は客数が少ないため、たばこのカートン買いなどで客単価が跳ねる。これは正常。
**必ず時間帯別のしきい値を使うこと。** 固定値にすると深夜が警告だらけになる。

### 2-3. 客数が少ない時間は対象外にする

**客数10人未満の時間は判定しない。** 上記のカートン買い問題を避けるため。
この条件により、深夜2〜4時はほぼ自動的に対象外になる。

---

## 3. スコープ

### やること

1. しきい値定数の追加（Step 1）
2. 一括入力モーダルに客単価表示とセル単位の注意表示（Step 2）
3. 保存時の1日合計チェック（Step 3）

### やらないこと

- **保存のブロック**（警告のみ。必ず保存できること）
- 自動補正・自動除外
- Firestore のデータ構造変更（保存する内容は現行のまま）
- **Firestore セキュリティルール・Firebase 設定の変更**（他アプリと共用のプロジェクトのため厳禁）
- 日販廃棄アプリ側の変更

---

## 4. しきい値（実測値。そのまま使うこと）

出典：`hourly_metrics` の2025-10-08〜2026-08-02、取り違え7件を除いた810日分から算出（2026-08-04時点）。

### 4-1. 時間帯別（全店共通・客数10人以上の時間のみ・1%〜99%点）

```js
// [下限, 上限]（円）。null は判定対象外
export const HOURLY_SPEND_RANGE = {
  0:  [300, 1583],  1:  [125, 1923],  2:  null,         3:  null,
  4:  null,         5:  [286, 1417],  6:  [333, 1333],  7:  [400, 1200],
  8:  [379, 1160],  9:  [355, 1455],  10: [433, 2000],  11: [476, 1483],
  12: [483, 1241],  13: [400, 1391],  14: [386, 1545],  15: [360, 1300],
  16: [417, 1350],  17: [500, 1440],  18: [508, 1414],  19: [524, 1500],
  20: [556, 1442],  21: [536, 1412],  22: [524, 1722],  23: [448, 1786],
};
export const HOURLY_SPEND_MIN_CUSTOMERS = 10;  // これ未満の客数の時間は判定しない
```

2〜4時が `null` なのは、客数10人以上のデータが30件未満で統計的に信頼できないため。

### 4-2. 1日合計（店舗別・2%〜98%点）

```js
// 店舗名 → [下限, 上限]（円）
export const DAILY_SPEND_RANGE = {
  '伊賀平野東町店':   [736, 989],
  '伊賀平野北谷店':   [737, 990],
  '伊賀忍者市駅南店': [635, 834],
};
export const DAILY_SPEND_RANGE_DEFAULT = [635, 990];  // 未登録店舗のフォールバック
```

**店舗IDではなく店舗名でキーを引くこと。** 店舗IDは環境によって変わるため。
`masterData.stores` から現在の店舗名を解決する。未登録の店舗名なら `DAILY_SPEND_RANGE_DEFAULT` を使う。

---

## 5. 実装

### Step 1：しきい値定数の追加

`lib/utils.js` に上記4-1・4-2の定数を追加する。

あわせて判定関数を追加：

```js
// 時間帯単位の判定。範囲外なら true
export const isHourlySpendAbnormal = (hour, customers, sales) => {
    if (typeof customers !== 'number' || typeof sales !== 'number') return false;
    if (customers < HOURLY_SPEND_MIN_CUSTOMERS) return false;
    const range = HOURLY_SPEND_RANGE[hour];
    if (!range) return false;
    const spend = sales / customers;
    return spend < range[0] || spend > range[1];
};

// 1日合計の判定
export const checkDailySpend = (storeName, custSum, salesSum) => {
    if (!custSum || !salesSum) return null;
    const range = DAILY_SPEND_RANGE[storeName] || DAILY_SPEND_RANGE_DEFAULT;
    const spend = salesSum / custSum;
    return { spend: Math.round(spend), range, abnormal: spend < range[0] || spend > range[1] };
};
```

### Step 2：一括入力モーダルの改修

対象：`screens/TimetableScreen.jsx` の `MetricsBulkInputModal`（8行目付近）

#### 2-1. 客単価の列を追加

現在の表は「時間 / 客数 (人) / 販売金額 (円)」の3列。**右端に「客単価」列を追加**して4列にする。

- 客数と売上が両方入力されている行のみ表示。それ以外は空欄
- 表示は `Math.round(sales / customers)` に「円」を付ける
- 客数が `HOURLY_SPEND_MIN_CUSTOMERS` 未満の行は、グレーの小さめ文字にする（判定対象外であることが分かるように）

#### 2-2. 範囲外のセルに注意表示

`isHourlySpendAbnormal()` が `true` の行は、**客単価セルの文字色を黄色（`text-yellow-400`）にし、行の背景を `bg-yellow-900/30` にする。**

このアプリはダークテーマ（`bg-gray-800` 基調）なので、明るい黄色背景は使わないこと。

行末に `⚠️` を付ける。ツールチップ（`title` 属性）に「この時間帯の通常範囲は ◯◯〜◯◯円です」と出す。

**入力は妨げない。** フォーカス移動（Enterで次の行へ）の挙動は現行のまま変更しないこと。

#### 2-3. 合計行の追加

表の最下部に合計行を追加する。

```
合計 | 客数◯◯人 | 売上◯◯円 | 客単価◯◯円
```

`checkDailySpend()` が `abnormal: true` なら、この行を黄色系で強調する。

### Step 3：保存時の1日合計チェック

`handleSaveClick`（38行目付近）を改修する。

現行は `cleanedData` を作って `onSave(cleanedData)` を呼ぶだけ。ここに判定を挟む。

```js
const handleSaveClick = () => {
    const cleanedData = {}; /* ...現行のまま... */

    // ▼ 追加：1日合計の客単価チェック
    const result = checkDailySpend(storeName, custSum, salesSum);
    if (result && result.abnormal) {
        const ok = window.confirm(
            `1日の客単価が通常の範囲から外れています。\n\n` +
            `客単価：${result.spend.toLocaleString()}円\n` +
            `この店舗の通常範囲：${result.range[0].toLocaleString()}〜${result.range[1].toLocaleString()}円\n\n` +
            `売上と客数が入れ替わっていないかご確認ください。\n` +
            `クリスマスや大晦日など、実際に客単価が上がる日もあります。\n` +
            `問題なければ「OK」で保存します。`
        );
        if (!ok) return;   // キャンセル時は保存せずモーダルに留まる
    }

    onSave(cleanedData);
};
```

**「OK」を押せば必ず保存されること。** 2度目の確認や、保存を諦めさせる導線を作らないこと。

`storeName` は `currentUser.storeId` から `masterData.stores` を引いて解決する。モーダルに props で渡すこと。

---

## 6. 受け入れ条件

### 機能

- [ ] 一括入力モーダルに客単価列が表示される
- [ ] 客数10人未満の行は客単価がグレー表示になり、⚠️ が付かない
- [ ] 範囲外の行が黄色く強調され、⚠️ とツールチップが出る
- [ ] 合計行に1日の客数・売上・客単価が出る
- [ ] 保存時、1日客単価が範囲外なら確認ダイアログが出る
- [ ] **ダイアログでOKを押すと保存される**
- [ ] ダイアログでキャンセルを押すと保存されず、入力内容が保持される
- [ ] Enterキーでのフォーカス移動が従来どおり動く

### 実データでの検証

以下は調査済みの確定値。**この通りになれば正しく動いている。**

**必ず警告が出るべき日（取り違え7件）** — 1日客単価チェックで全件検出されること

| 日付 | 店舗 | 売上 | 客数 | 客単価 |
|---|---|---|---|---|
| 2025-11-27 | 伊賀平野東町店 | 779,000 | 589 | 1,323円 |
| 2025-11-28 | 伊賀平野東町店 | 797,000 | 652 | 1,222円 |
| 2025-12-27 | 伊賀平野東町店 | 781,000 | 621 | 1,258円 |
| 2026-03-07 | 伊賀平野東町店 | 841,000 | 682 | 1,233円 |
| 2026-04-05 | 伊賀平野北谷店 | 646,000 | 646 | 1,000円 |
| 2026-05-23 | 伊賀平野東町店 | 909,000 | 749 | 1,214円 |
| 2026-07-24 | 伊賀平野東町店 | 887,000 | 718 | 1,235円 |

※2026-04-05は上限990円に対して1,000円とギリギリ。ここが検出できるかが実装の正しさの判定点になる。

**警告が出るが、それで正しい日（本物のイベント）**

| 日付 | 店舗 | 客単価 |
|---|---|---|
| 2025-12-24 | 伊賀平野東町店 | 1,148円 |
| 2025-12-31 | 伊賀平野東町店 | 1,078円 |
| 2026-03-31 | 伊賀平野北谷店 | 1,066円 |

これらでもダイアログが出て、**OKを押せば保存できる**こと。

**誤警告の目安**：正常日810日のうち約4%（32日）で1日チェックのダイアログが出る。月1回程度。これは想定内であり、しきい値を締める方向に調整しないこと。

---

## 7. 禁止事項

1. **保存のブロック** — 警告は出すが、OKを押せば必ず保存できること
2. **Firestore セキュリティルール・Firebase 設定の変更** — 他アプリと共用のプロジェクトのため厳禁
3. **保存データの構造変更** — `hourlyData` の形式は現行のまま。客単価を保存フィールドに追加しない
4. **自動補正** — 範囲外の値を勝手に直さない
5. **しきい値の独自調整** — 本書の実測値をそのまま使う
6. **店舗IDのハードコード** — 店舗名で引くこと
7. **フォーカス移動の挙動変更** — Enterでの移動順は現行のまま

---

## 8. バージョン

`lib/utils.js` の `APP_VERSION` を `'1.4.7'` → `'1.5.0'` に更新する。

---

## 9. 補足：しきい値の見直しについて

このしきい値は10か月分のデータから算出したものです。季節変動や商品構成の変化でずれていくため、**年1回程度の再計算**を想定しています。

そのため、しきい値は `lib/utils.js` の定数として**1か所にまとめて**定義し、後から差し替えやすくしてください。ロジックの中に数値を直接書かないこと。
